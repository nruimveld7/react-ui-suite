{
  "name": "react-ui-suite",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace @react-ui-suite/core",
    "dev": "npm run dev --workspace demo"
  },
  "workspaces": [
    "src",
    "demo"
  ]
}
