{
  "name": "react-ui-suite",
  "private": true,
  "workspaces": [
    "src",
    "demo"
  ],
  "scripts": {
    "build": "npm --workspace src run build",
    "dev": "npm --workspace demo run dev",
    "lint": "npm --workspace src run lint",
    "test": "npm --workspace src run test"
  }
}
